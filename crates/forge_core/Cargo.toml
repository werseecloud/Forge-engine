[package]
name = "forge_core"
version = "1.0.0"
edition = "2021"

[features]
default = []
editor = []

[dependencies]
glam = { version = "0.29", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
thiserror = "1"
tracing = "0.1"
uuid = { version = "1", features = ["v4", "serde"] }
