# Local Content Packs

Forge Engine Setup scans real local folders under `engine/` and includes them as optional installable components.

Large packs are supported by design:

- `engine/StarterContent`
- `engine/Shaders`
- `engine/Templates`
- `engine/Runtime`
- `docs`
- `licenses`

Component sizes are calculated from disk at installer runtime.

