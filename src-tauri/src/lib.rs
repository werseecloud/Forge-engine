pub mod commands;
pub mod models;
pub mod services;
pub mod utils;

pub fn run() {
    tauri::Builder::default()
        .plugin(tauri_plugin_dialog::init())
        .invoke_handler(tauri::generate_handler![
            commands::fs_commands::ensure_app_directories,
            commands::fs_commands::choose_directory,
            commands::fs_commands::choose_files,
            commands::fs_commands::read_directory_tree,
            commands::fs_commands::reveal_in_explorer,
            commands::fs_commands::watch_project_directory,
            commands::project_commands::create_project,
            commands::project_commands::open_project,
            commands::project_commands::close_project,
            commands::project_commands::list_recent_projects,
            commands::project_commands::pin_project,
            commands::project_commands::unpin_project,
            commands::project_commands::reveal_project_in_explorer,
            commands::project_commands::validate_project_path,
            commands::project_commands::repair_project_path,
            commands::asset_commands::import_assets,
            commands::asset_commands::scan_assets,
            commands::asset_commands::rebuild_asset_index,
            commands::asset_commands::get_asset_metadata,
            commands::asset_commands::update_asset_metadata,
            commands::asset_commands::delete_asset,
            commands::asset_commands::rename_asset,
            commands::asset_commands::move_asset,
            commands::asset_commands::duplicate_asset,
            commands::scene_commands::create_level,
            commands::scene_commands::open_level,
            commands::scene_commands::save_level,
            commands::scene_commands::list_levels,
            commands::scene_commands::update_scene_object,
            commands::scene_commands::add_scene_object,
            commands::scene_commands::delete_scene_object,
            commands::scene_commands::select_scene_object,
            commands::settings_commands::get_settings,
            commands::settings_commands::update_settings,
            commands::settings_commands::reset_settings,
            commands::settings_commands::set_default_projects_dir,
            commands::log_commands::read_output_log,
            commands::log_commands::append_output_log,
            commands::log_commands::clear_output_log
            ,
            commands::system_commands::run_system_check,
            commands::system_commands::check_existing_install,
            commands::system_commands::get_windows_user_paths,
            commands::system_commands::calculate_available_disk_space,
            commands::system_commands::validate_install_path,
            commands::system_commands::validate_project_folder,
            commands::component_commands::scan_available_components,
            commands::component_commands::calculate_component_size,
            commands::component_commands::validate_component_sources,
            commands::component_commands::get_component_install_plan,
            commands::installer_commands::create_install_plan,
            commands::installer_commands::run_install_plan,
            commands::installer_commands::cancel_install,
            commands::installer_commands::repair_install,
            commands::installer_commands::update_install,
            commands::installer_commands::uninstall_install,
            commands::folder_commands::create_user_folders,
            commands::folder_commands::create_install_folders,
            commands::folder_commands::open_folder,
            commands::manifest_commands::write_manifest,
            commands::manifest_commands::read_manifest,
            commands::manifest_commands::write_version_file,
            commands::manifest_commands::read_version_file,
            commands::installer_settings_commands::write_default_settings,
            commands::installer_settings_commands::read_installer_settings,
            commands::installer_settings_commands::update_installer_settings,
            commands::shortcut_commands::create_desktop_shortcut,
            commands::shortcut_commands::create_start_menu_shortcuts,
            commands::association_commands::register_file_associations,
            commands::association_commands::unregister_file_associations,
            commands::health_commands::run_component_health_checks,
            commands::health_commands::run_single_component_health_check,
            commands::installer_log_commands::read_installer_log,
            commands::installer_log_commands::append_installer_log,
            commands::installer_log_commands::clear_installer_log,
            commands::engine_commands::start_engine_services,
            commands::worker_commands::start_worker,
            commands::worker_commands::stop_worker,
            commands::worker_commands::restart_worker,
            commands::worker_commands::run_worker_health_check,
            commands::worker_commands::get_worker_status
        ])
        .run(tauri::generate_context!())
        .expect("failed to run Forge Engine editor");
}
